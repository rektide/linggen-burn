[package]
name = "linggen"
version = "0.10.1"
edition = "2021"
license = "Apache-2.0"
description = "Linggen — the root system for AI agents. A local-first runtime for AI apps."
repository = "https://github.com/linggen/linggen"

[features]
# Anonymous usage telemetry (install + command events sent to
# linggen.dev/api/track). See src/telemetry/mod.rs for the exact field list.
# Disable at runtime: env LINGGEN_NO_TELEMETRY=1 OR file ~/.linggen/no-telemetry.
# Disable at compile time: cargo build --no-default-features.
default = ["telemetry"]
telemetry = []
burn = ["dep:burn", "dep:burn-store", "dep:hf-hub", "dep:tokenizers", "burn-ndarray"]
burn-ndarray = ["burn/ndarray"]
burn-wgpu = ["burn/wgpu"]
burn-cuda = ["burn/cuda"]
burn-rocm = ["burn/rocm"]

[[bin]]
name = "ling"
path = "src/main.rs"

[dependencies]
anyhow = "1"
async-trait = "0.1"
clap = { version = "4", features = ["derive"] }
ignore = "0.4"
globset = "0.4"
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "process", "time"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "stream", "cookies"] }
uuid = { version = "1", features = ["v4"] }
headless_chrome = "1.0"
base64 = "0.22"
axum = "0.8"
tower-http = { version = "0.6", features = ["fs", "cors"] }
rust-embed = "8.0"
tokio-stream = "0.1"
futures-util = "0.3"
mime_guess = "2.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "chrono"] }
tracing-appender = "0.2"
toml = "1.0"
serde_yml = "0.0.12"
dirs = "6.0"
notify = "8"
grep = "0.4"
grep-regex = "0.1"
grep-searcher = "0.1"
tokio-util = { version = "0.7", features = ["codec"] }
tempfile = "3"
zip = "8"
url = "2"
libc = "0.2"
rand = "0.10.0"
cron = "0.16"
chrono = "0.4"
str0m = "0.19"
gethostname = "1.1.0"
open = "5.3.3"
urlencoding = "2.1.3"
flate2 = "1.1.9"
burn = { version = "0.21", default-features = false, features = ["std", "store"], optional = true }
burn-store = { version = "0.21", optional = true }
hf-hub = { version = "1.0.0-rc.1", features = ["blocking", "rustls-tls"], optional = true }
tokenizers = { version = "0.22", default-features = false, features = ["onig"], optional = true }
